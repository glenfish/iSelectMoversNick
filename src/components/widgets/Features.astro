---
import { Icon } from 'astro-icon';

const items = [
	[
		{
			title: 'Get the best price.',
			description:
				'With iSelect Movers, you get a choice of removalists all out bidding each other.',
			icon: 'tabler:list-check',
		},
		{
			title: 'All removalists are vetted',
			description:
				'You get the iSelect Movers assurance. All removalists are vetted, so you get a quality move.',
			icon: 'tabler:list-check',
		},
		{
			title: 'Expeirenced removalists',
			description:
				'All removalists are expeirenced and qualified.',
			icon: 'tabler:list-check',
		},
	],
	[
		{
			title: 'Get other services',
			description:
				'Organise other services like bond cleaning and yard maintenance through your removalist.',
			icon: 'tabler:list-check',
		},
		{
			title: 'Speak with a real person',
			description:
				'Speak with your removalist before you lock-it-in for peace of mind and the very best price.',
			icon: 'tabler:list-check',
		},
		{
			title: 'Save your back',
			description:
				'Full loading and unloading is part of every service, so you can relax and save your back and your goods.',
			icon: 'tabler:list-check',
		},
	],
];
---

<section class="scroll-mt-16" id="features">
	<div class="px-4 py-16 mx-auto max-w-6xl lg:px-8 lg:py-20">
		<div class="mb-10 md:mx-auto sm:text-center md:mb-12 max-w-3xl">
			<p class="text-base text-orange-600 dark:text-primary-200 font-semibold tracking-wide uppercase">Features</p>
			<h2 class="text-4xl md:text-5xl font-bold leading-tighter tracking-tighter mb-4 font-heading">
				What you with <span class="whitespace-nowrap">iSelect Movers</span>
			</h2>
			<p class="max-w-3xl mx-auto sm:text-center text-xl text-gray-600 dark:text-slate-400">
				We take that headaches out of moving. In just a few minutes, your moving day is all sorted. 
			</p>
		</div>
		<div class="grid mx-auto space-y-6 md:grid-cols-2 md:space-y-0">
			{
				items.map((subitems) => (
					<div class="space-y-8 sm:px-8">
						{subitems.map(({ title, description, icon }) => (
							<div class="flex flex-row max-w-md">
								<div class="mb-4 mr-4">
									<div class="flex items-center justify-center w-12 h-12 rounded-full bg-orange-500 dark:bg-orange-500">
										{icon && <Icon name={icon} class="w-6 h-6 text-white icon-light" />}
									</div>
								</div>
								<div>
									<h3 class="mb-3 text-xl font-bold">{title}</h3>
									<p class="text-gray-600 dark:text-slate-400">{description}</p>
								</div>
							</div>
						))}
					</div>
				))
			}
		</div>
	</div>
</section>
